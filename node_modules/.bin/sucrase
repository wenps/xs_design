#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_sucrase@3.32.0@sucrase/bin/sucrase" "$@"
  ret=$?
else 
  node  "$basedir/../_sucrase@3.32.0@sucrase/bin/sucrase" "$@"
  ret=$?
fi
exit $ret
